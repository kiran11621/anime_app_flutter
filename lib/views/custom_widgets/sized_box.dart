import 'package:flutter/material.dart';

class SizedBoxHeight5 extends StatelessWidget {
  const SizedBoxHeight5({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 5,
    );
  }
}

class SizedBoxHeight10 extends StatelessWidget {
  const SizedBoxHeight10({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 10,
    );
  }
}
